// TODO: Fase 1 — Meus Cursos
// Lista todos os cursos liberados para o aluno com progresso
// Se não tiver cursos: "Você ainda não possui cursos liberados" + link para planos

export default function MeusCursosPage() {
  return (
    <div className="p-6 md:p-8">
      <h1 className="text-2xl font-bold text-foreground mb-8">Meus Cursos</h1>
      {/* TODO: fetch cursos liberados via getUserAccess() + renderizar CourseCard grid */}
      {/* Empty state: "Você ainda não possui cursos liberados." + botão Ver Planos */}
    </div>
  )
}
