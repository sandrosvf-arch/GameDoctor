// TODO: Admin — Gestão de Alunos
// Busca por nome/e-mail, ver cursos, plano ativo, progresso, histórico de compras
// Ações: liberar acesso, bloquear, resetar senha, ver último acesso

export default function AdminAlunosPage() {
  return (
    <div className="p-6 md:p-8">
      <h1 className="text-2xl font-bold text-foreground mb-8">Alunos</h1>
      {/* TODO: Busca + filtros */}
      {/* TODO: DataTable com alunos — nome, e-mail, plano, status, último acesso, ações */}
    </div>
  )
}
